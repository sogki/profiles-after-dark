web: node start-both.js

# For Railway deployment:
# - 'web' process runs both API server and Discord bot together
# - This ensures both services start even if Railway doesn't run worker processes
# - Railway will automatically route traffic to the 'web' process


